# Versions are those used for the thesis experiments (Python 3.12).
#
# NOTE ON OpenAttack: the experiments used a local clone of
# https://github.com/thunlp/OpenAttack rather than a PyPI release, because the
# published package lagged behind the fixes needed for these attack recipes.
# Install it from source:
#     git clone https://github.com/thunlp/OpenAttack.git
#     pip install -e OpenAttack

torch==2.2.1
transformers==4.38.2
datasets==2.18.0
textattack==0.3.10
tensorflow==2.16.1          # required by OpenAttack's Universal Sentence Encoder metric
sentence-transformers        # MiniLM similarity (section 7)
language_tool_python==2.7.3  # grammatical-error metric
nltk==3.8.1                  # METEOR score
gensim==4.3.2
numpy==1.26.4
scipy==1.12.0
pandas==2.2.1
scikit-learn==1.4.1.post1
sentencepiece==0.1.99
certifi==2024.2.2
openpyxl                     # writing the .xlsx result workbooks
jupyter
